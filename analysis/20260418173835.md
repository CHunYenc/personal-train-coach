# 跑步紀錄分析：`data/20260418173835.fit`

本稿僅依 `coach-parse --json` 產出之 `session.json` 可驗證欄位撰寫；未出現在該檔的週跑量、心率區間門檻、睡眠或 HRV 等一律視為**未知**，不臆測數值。

## 資料摘要（來自 `session.json`）

- **檔案**：`data/20260418173835.fit`（解析路徑與檔名一致）。
- **項目**：`running`。
- **活動時間（UTC）**：`start_time` 2026-04-18 09:38:35+00:00 至 `timestamp` 2026-04-18 10:18:20+00:00（單次連續紀錄之起訖）。
- **時間與距離**：`total_elapsed_time` / `total_timer_time` 均為 2385.43 s；`total_distance` 4766.21 m。
- **心率**：`avg_heart_rate` 147、`max_heart_rate` 160、`min_heart_rate` 90。
- **速度**：`enhanced_avg_speed` / `avg_speed` 1.998 m/s；`enhanced_max_speed` / `max_speed` 3.012 m/s。
- **步態與負荷相關**：`avg_running_cadence` 78、`max_running_cadence` 91、`avg_step_length` 760.0、`total_strides` 3093；`avg_power` 135；`avg_stance_time` 285.0；`avg_vertical_oscillation` 88.0；`avg_vertical_ratio` 11.4；另有 `Effort Pace` 約 2.004（解讀方式未在 JSON 中定義，以下不延伸為生理判讀）。
- **環境與地形**：`avg_temperature` 29；`total_ascent` 5、`total_descent` 6（單位依裝置；此處僅照抄）。
- **裝置識別**：`file_id` 顯示 `manufacturer` coros、`product_name` COROS PACE 4；`time_created` 2026-05-02 09:03:37+00:00 為檔案／匯出時間戳，**不等於**跑步開始時間（跑步開始以上列 `start_time` 為準）。

## 教練觀點（非醫療、非診斷）

### 單次課表定位

在僅有此一筆紀錄的前提下，這是一堂約 **39.8 分鐘**、距離約 **4.77 km** 的連續跑。平均心率 **147**、最高 **160**，搭配平均速度約 **2.0 m/s**，就數據外觀而言偏向**穩定節奏的有氧主導課表**；是否落在個人「輕鬆跑／有氧基礎」區，**取決於個人最大心率或閾值心率（此檔未提供）**——以下若談到「Zone 2」或「輕鬆區」均為**假設**，需用自覺疲勞（RPE）或實測門檻校準。

### 與馬拉松週期化（一般原則＋本堂可讀訊息）

馬拉松準備通常區分為有氧打底、強度與長距離的漸進堆疊，以及賽前調整。**本 repository 本次解析未提供週跑量、長課距離或連續數週趨勢**，因此無法評論「週量是否上升過快」或「恢復是否足夠」——只能陳述本堂內在訊號：

- **有氧基礎**：在假設 147 bpm 對你屬於可控、可對話強度的前提下，這類中等時數、非極端尖峰心率的跑步，常用於維持心肺與跑步經濟性的底層刺激。若賽前仍處打底期，此強度分佈（單次平均相對於最大值的比例）**看起來**不像短間歇或反覆無氧衝刺；但仍建議用主觀輕鬆度與隔日恢復感驗證，而非僅看平均值。
- **強度分佈**：僅有單次平均／極值，**無法**還原「Zone 1–5 分鐘比例」。若要優化週期內強度配比，需要更多檔案或手動紀錄輕鬆日、節奏跑、閾值與間歇的比例。
- **恢復**：沒有隔日 HRV、靜息心率或睡眠欄位，**無法**從本檔推論恢復狀態。實務上建議：若隔日主觀疲勞偏高或靜息心率異常（若有量測），下一日應優先降強度或縮量，而非硬套原課表。
- **週量趨勢**：**未知**（單檔）。週期化決策應以連續週的總量、最長單次、以及最近三週變化率為主；單此一筆僅能當作「其中一堂維持跑」的樣本。

### 跑步力學與裝置讀數（謹慎解讀）

`avg_vertical_ratio` 11.4、`avg_vertical_oscillation` 88.0、`avg_stance_time` 285.0 等為穿戴裝置估計值，受校準、路面與配速影響。**不**將其標籤為「錯誤跑姿」或傷害風險結論；若你要優化，建議在**同一路線、相近配速**下比較多次趨勢，或搭配教練／物理治療師現場觀察（非本分析範圍）。

### 實務後續（可執行、仍帶假設）

1. **校準強度**：以 RPE 6–7／可簡短對話為「輕鬆有氧」自查；若 147 bpm 對應喘不過氣，則假設「這是輕鬆區」不成立，應降速。
2. **補齊週期化所需資訊**：至少紀錄每週總距離／總時間、最長一次 LSD、以及任一強度日；再討論是否需加量或加強度。
3. **熱與補給**：`avg_temperature` 29 表示環境偏暖（假設單位為攝氏且感測合理），長課時需注意補水與電解質；本堂長度與距離屬中等，仍屬一般衛教提醒，**非**依據醫療評估。

---

*若 `session.json` 更新或加入多週檔案，本敘事應重新對齊資料後再解讀。*
